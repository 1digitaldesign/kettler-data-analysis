# Python Requirements for DPOR Data Analysis
# Install with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Hugging Face transformers for data cleaning
transformers>=4.30.0
torch>=2.0.0

# Vector embeddings and ETL
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
huggingface-hub>=0.20.0

# PDF processing (multiple options for compatibility)
PyPDF2>=3.0.0
pdfplumber>=0.10.0
pypdf>=3.0.0

# Excel processing
openpyxl>=3.1.0
xlrd>=2.0.0

# API server dependencies
flask>=2.3.0
flask-cors>=4.0.0

# Docker integration
docker>=6.1.0

# Google Cloud Platform and Vertex AI
google-cloud-aiplatform>=1.38.0
google-cloud-storage>=2.10.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Google Drive API
google-api-python-client>=2.100.0
google-api-core>=2.11.0

# Web scraping (for search scripts conversion)
requests>=2.31.0
beautifulsoup4>=4.12.0
selenium>=4.15.0

# Web API server
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Accuracy-focused tools for Hugging Face Hub datasets
# Speed is NOT a concern - accuracy and precision are paramount

# Data validation (CRITICAL for Hugging Face Hub)
great-expectations>=0.18.0

# Most accurate HTML parsing
html5lib>=1.1
lxml>=5.0.0

# Accurate data types for Hugging Face Hub
pyarrow>=14.0.0

# Persistent vector storage with validation
qdrant-client>=1.7.0

# Anti-bot detection and CAPTCHA handling
undetected-chromedriver>=3.5.0  # Undetectable Chrome driver
python-anticaptcha>=1.0.0  # CAPTCHA solving (optional)
2captcha-python>=1.1.0  # 2Captcha service integration (optional)
anticaptchaofficial>=1.0.0  # AntiCaptcha service (optional)

# Parallel processing with rate limiting
concurrent-futures>=3.1.1  # Usually built-in, but explicit for clarity

# High-accuracy API services (choose one)
# scraperapi>=1.0.0  # BEST ACCURACY - 99.9%+ success rate
# zenrows>=1.0.0  # Good accuracy alternative

# Optional: for advanced NER models
# sentencepiece>=0.1.99
# protobuf>=3.20.0
