# GCP Configuration Template
# Copy these values to your .env file

# GCP Project ID
GCP_PROJECT_ID=claude-eval-20250615

# GCP Region (default: us-central1)
GCP_REGION=us-central1

# GCP Service Account Email
GCP_CLIENT_EMAIL=fraud-analysis@claude-eval-20250615.iam.gserviceaccount.com

# GCP Credentials JSON (optional - if using file, set GOOGLE_APPLICATION_CREDENTIALS instead)
# GCP_CREDENTIALS_JSON={"type":"service_account",...}

# Note: The credentials file is located at config/gcp-credentials.json
# GOOGLE_APPLICATION_CREDENTIALS is automatically set in docker-compose.yml
